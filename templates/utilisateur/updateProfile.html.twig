{% extends 'base.html.twig' %}

{% block title %}{{ utilisateur.nom ~ utilisateur.prenom}} | {{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="h1-romain">{{utilisateur.prenom}}{{ app.user.id }}</h2>
        <div class="profile-image">
            <img src="#" title="img-profile2~{{ utilisateur.nom }}">
        </div>

        <div class="form-detailUtilisateur">
            {{ form_start(updateForm) }}
            {{ form_widget(updateForm) }}
                <button type="submit" class="btn">Mettre à jour le profil</button>
            {{ form_end(updateForm) }}
        </div>
    </div>
{% endblock %}