{% extends 'base.html.twig' %}

{% block title %}Edit Lieu{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="h1-romain">Mettre à jour le lieu</h1>

    {{ include('lieu/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="details-link" href="{{ path('lieu_index') }}">Retour à la liste des lieux</a>

    {{ include('lieu/_delete_form.html.twig') }}
    </div>
{% endblock %}
