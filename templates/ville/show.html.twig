{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="h1-romain">Ville</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
            <tr>
                <th>Code Postal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>

    <a class="details-link" style="border-right: black solid 1px;padding-right: 0.5em" href="{{ path('ville_index') }}">Retour à la liste des villes</a>

    <a class="details-link" href="{{ path('ville_edit', {'id': ville.id}) }}">Modifier</a>

    {{ include('ville/_delete_form.html.twig') }}
    </div>
{% endblock %}
