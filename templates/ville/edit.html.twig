{% extends 'base.html.twig' %}

{% block title %}Edit Ville{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="h1-romain">Modifier la ville</h1>

    {{ include('ville/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="details-link mt-3" href="{{ path('ville_index') }}">Retour à la liste</a>

    {{ include('ville/_delete_form.html.twig') }}
    </div>
{% endblock %}
