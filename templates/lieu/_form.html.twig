{{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('ville_new') }}" class="btn btn-dark mt-3">Ajouter une ville</a>
    <button class="btn btn-dark mt-3 ">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
