{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="container">
    <h1 class="h1-romain h3 mb-3 font-weight-normal">Enregistrer un nouveau participant</h1>

    {{ form_start(registrationForm) }}
        {{ form_widget(registrationForm) }}
        <button type="submit" class="btn btn-primary">Enregistrer le participant</button>
    {{ form_end(registrationForm) }}
    </div>
{% endblock %}
